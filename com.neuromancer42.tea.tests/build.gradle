def resolveTask = tasks.named("resolve.com.neuromancer42.tea.tests") {
    outputBndrun = layout.buildDirectory.file("com.neuromancer42.tea.tests.bndrun")
}
tasks.named("testOSGi") {
    it.doFirst {
        copy {
            from 'resources/'
            into temporaryDir
        }
    }
    bndrun = resolveTask.flatMap { it.outputBndrun }
}