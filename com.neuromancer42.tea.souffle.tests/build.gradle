def resolveTask = tasks.named("resolve.com.neuromancer42.tea.souffle.tests") {
    outputBndrun = layout.buildDirectory.file("com.neuromancer42.tea.souffle.tests.bndrun")
}
tasks.named("testOSGi") {
    it.doFirst {
        copy {
            from 'resources/'
            into temporaryDir
        }
    }
    bndrun = resolveTask.flatMap { it.outputBndrun }
}